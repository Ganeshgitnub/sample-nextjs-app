version: 0.0
os: linux
files:
  - source: /
    destination: /root/frontend
    overwrite: true
hooks:
  BeforeInstall:
    - location: aws/before_install.sh
      timeout: 300
      runas: root
  AfterInstall:
    - location: aws/after_install.sh
      timeout: 300
      runas: root
